<h1> All tags </h1>

<ul id="tags">
    <% @tags.each do |tag| %>
        <li>
    <%= link_to tag.title, tag_path(tag)
        </li>
    <% end %>
</ul>

    # need to complete the final topic on chapter I3.